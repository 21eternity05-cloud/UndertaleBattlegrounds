-- TODO: Install the official TopbarPlus v3 Icon module here when package import is available.
-- Preferred path: ReplicatedStorage > Packages > TopbarPlus
-- The client Topbar UI falls back to lightweight buttons until that module exists.

return {
	Package = "TopbarPlus",
	PreferredPath = "ReplicatedStorage.Packages.TopbarPlus",
	ApiStyle = "TopbarPlus v3 Icon.new()",
}
